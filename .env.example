# ============================================
# Environment Configuration
# ============================================
# Copy this file to backend/.env and update with your values
# IMPORTANT: backend/.env is gitignored - never commit secrets

# ============================================
# Node Environment
# ============================================
# Options: development, production, test
NODE_ENV=development

# ============================================
# Server Configuration
# ============================================
# Port the Express server will listen on
PORT=3000

# Host to bind to (use 0.0.0.0 to accept external connections)
HOST=localhost

# ============================================
# Database Configuration
# ============================================
# PostgreSQL connection string
# Format: postgresql://[user[:password]@][host][:port][/dbname][?param1=value1&...]
# Example: postgresql://myuser:mypassword@localhost:5432/mydb
DATABASE_URL=postgresql://user:password@localhost:5432/dbname

# Connection pool settings
# Minimum number of connections to maintain
DATABASE_POOL_MIN=2

# Maximum number of connections in the pool
DATABASE_POOL_MAX=10

# ============================================
# Authentication
# ============================================
# Secret key for signing JWT tokens
# SECURITY: Generate a strong random string (minimum 32 characters)
# Example: openssl rand -base64 32
JWT_SECRET=your-secret-key-change-this-in-production

# JWT token expiration time
# Examples: 60, "2 days", "10h", "7d"
JWT_EXPIRES_IN=7d

# ============================================
# CORS Configuration
# ============================================
# Allowed origin for CORS requests
# In production, set this to your frontend domain
# Examples: https://example.com, https://app.example.com
CORS_ORIGIN=http://localhost:3000

# ============================================
# Logging
# ============================================
# Logging level for application logs
# Options: error, warn, info, http, verbose, debug, silly
LOG_LEVEL=info

# ============================================
# Error Tracking
# ============================================
# Sentry DSN for error tracking (optional — leave blank to disable)
# Get your DSN from https://sentry.io
# SENTRY_DSN=https://examplePublicKey@o0.ingest.sentry.io/0

# ============================================
# Optional: Additional Configuration
# ============================================

# Session secret for cookie-based sessions (if using express-session)
# SESSION_SECRET=another-secret-key-change-this

# Redis connection for session storage (if using)
# REDIS_URL=redis://localhost:6379

# Email service configuration (if using)
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=your-email@example.com
# SMTP_PASS=your-email-password
# EMAIL_FROM=noreply@example.com

# File upload limits
# MAX_FILE_SIZE=10485760  # 10MB in bytes

# Rate limiting
# RATE_LIMIT_WINDOW_MS=900000  # 15 minutes
# RATE_LIMIT_MAX_REQUESTS=100

# ============================================
# Production Checklist
# ============================================
# Before deploying to production:
# ✓ Set NODE_ENV=production
# ✓ Use strong DATABASE_URL with SSL enabled
# ✓ Generate secure JWT_SECRET (minimum 32 random characters)
# ✓ Set CORS_ORIGIN to your actual domain
# ✓ Configure proper LOG_LEVEL (info or warn)
# ✓ Enable database connection SSL
# ✓ Review and remove any unused variables
